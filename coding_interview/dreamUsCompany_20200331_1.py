# nums1, nums2 두 개의 정렬된 리스트가 주어졌을 때, 두 리스트를 병합하여 nums1에 저장한 후, nums1을 반환하십시오.
# Time complexity와 space complexity를 고려하여 최적화에 신경써주세요.
# 단, 병합된 리스트는 정렬되어 있어야합니다.
# len1, len2는 각각 nums1, nums2에서 유효한 값의 개수 입니다.
#
# 하지만 실제로 nums1은 len1 + len2 만큼의 길이를 가지고 있습니다.
# 매개변수들은 아래와 같은 조건을 만족합니다.
#
# 0 <= len1, len2 <= 1000
# len(nums2) == len2
# len(nums1) == len1 + len2
# Example 1
# Input: nums1=[1,2,3,0,0,0], len1=3, nums2=[2,5,6], len2=3
# Output: nums1=[1,2,2,3,5,6]

